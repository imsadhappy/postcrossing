<h1>Forgot your password?</h1>

<%= form_with(url: account_password_reset_path) do |form| %>
  <div>
    <%= form.label :email %>
    <%= form.email_field :email, required: true, autofocus: true %>
  </div>

  <div>
    <%= form.submit "Send password reset email" %>
  </div>
<% end %>
